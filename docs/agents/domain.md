# Domain docs

Before changing podcast-production code, terminology, issue wording, or architecture, read `CONTEXT.md` and any relevant records in `docs/adr/`. If either location is absent, proceed without creating placeholder documents.

Use the glossary's preferred terms in code, tests, issues, and design notes. If a proposal contradicts an existing ADR, identify the conflict explicitly instead of silently replacing the decision.
